_site
# Derived from git history at build time by scripts/build_feed_data.py.
# Committing it would mean every content commit needs a second commit to
# refresh the feed, so CI regenerates it instead.
_data/feed_entries.yml
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
_config_dev.yml

# Large files served from R2
*.pmtiles
londonriders/trains.json
